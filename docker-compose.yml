services:
  backend:
    build: ./backend
    ports:
      - "8000:80"
    depends_on:
      - db
    env_file:
      - .env
    # restart: always

  db:
    image: postgres:12
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # restart: always

volumes:
  postgres_data:
